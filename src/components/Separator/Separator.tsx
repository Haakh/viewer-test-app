import * as React from 'react';
import { View } from 'react-native';

export const Separator: React.SFC = () => {
  return <View style={{ height: 1, width: '80%', marginLeft: '10%', backgroundColor: 'grey' }} />;
};
